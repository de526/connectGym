<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- <mapper namespace="com.jasla.ConnectGym.mapper.GymMapper"> 인터페이스 자바 잡는거-->

<mapper namespace="mapper.BoardMapper"><!-- 이건 xml 잡는거고  -->
	

	<!-- 총 게시글 갯수 출력 -->
	<select id="countBoard" resultType="int">
		SELECT 
			COUNT(*) 
		FROM 
			BOARD_T
	</select>
	
	<!-- 페이징 처리 후 게시글 조회 -->
	<select id="selectBoard" resultType="com.jasla.ConnectGym.domain.BoardDTO" parameterType="com.jasla.ConnectGym.domain.PagingDTO">
		SELECT *
			FROM (
					SELECT ROWNUM RNO, A.*
						FROM (
										SELECT *
										FROM BOARD_T
										ORDER BY bo_no DESC
										) A
			) 
		WHERE RNO BETWEEN #{start} AND #{end}
	</select>
			
	
</mapper>